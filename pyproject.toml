[project]
name = "french-audiobook"
version = "0.1.0"
description = "Local French audiobook generation tool."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
